#ifndef PUZLES
#define PUZLES
#include "configuracion.h"

void puzle_morse(partida *p);
void puzle_switch(partida *p);
void puzle_despacho(partida *p);
void puzle_final(partida *p);

#endif // PUZLES
